% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/survival_estimation.R
\name{TNBsurvival}
\alias{TNBsurvival}
\title{Calculate Survival Estimates with Confidence Intervals}
\usage{
TNBsurvival(data, times, conf = FALSE, conf.level = 0.95)
}
\arguments{
\item{data}{A list containing survival estimates (output of \code{TNBintervals}).}

\item{times}{Numeric vector of times for which survival estimates are required.}

\item{conf}{Logical, whether to calculate confidence intervals. Default is \code{FALSE}.}

\item{conf.level}{Numeric, confidence level for intervals (e.g., 0.95 for 95\%). Default is 0.95.}
}
\value{
A data frame with columns:
\item{time}{Times at which survival is estimated.}
\item{survival}{Survival probabilities at the specified times.}
\item{ci_lower}{Lower bound of the confidence interval (if \code{conf = TRUE}).}
\item{ci_upper}{Upper bound of the confidence interval (if \code{conf = TRUE}).}
}
\description{
This function calculates survival probabilities for a given set of times and optionally adds confidence intervals.
}
\examples{
# Example data
left <- c(2, 4, 6, 8)
right <- c(4, 6, 8, NA)

# Create intervals with bootstrap
intervals <- TNBintervals(left, right, nboot = 10)

# Estimate survival probabilities
times <- c(1, 3, 5, 7)
survival_estimates <- TNBsurvival(intervals, times, conf = TRUE, conf.level = 0.95)
print(survival_estimates)
}
