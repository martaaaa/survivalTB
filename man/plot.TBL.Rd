% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{plot.TBL}
\alias{plot.TBL}
\title{Interactive Survival Plot with Plotly}
\usage{
\method{plot}{TBL}(
  x,
  conf = FALSE,
  conf.level = 0.95,
  main = "Survival Estimate",
  xlab = "Time",
  ylab = "Survival",
  line.col = "blue",
  showlegend = TRUE,
  main.line = "survival",
  filled = "CI",
  line.width = 2,
  fillcolor = "rgba(128, 128, 128, 0.3)",
  ...
)
}
\arguments{
\item{x}{A list containing survival estimates (output of \code{TNBintervals}).}

\item{conf}{Logical, whether to include confidence bands. Default is \code{FALSE}.}

\item{conf.level}{Numeric, confidence level for the bands (e.g., 0.95 for 95\%). Default is 0.95.}

\item{main}{Character, title of the plot.}

\item{xlab}{Character, label for the x-axis.}

\item{ylab}{Character, label for the y-axis.}

\item{line.col}{Character, color of the survival curve. Default is "blue".}

\item{showlegend}{Logical, whether to show the legend. Default is \code{TRUE}.}

\item{main.line}{Character, the label for the main survival curve in the plot legend. Default is "survival".}

\item{filled}{Character, the label for the confidence intervals ribbon in the plot legend. Default is "CI".}

\item{line.width}{Numeric, line width for the survival curve. Default is 2.}

\item{fillcolor}{Character, color for the confidence band fill (e.g., "rgba(128, 128, 128, 0.3)"). Default is grey.}

\item{...}{Additional graphical parameters.}
}
\value{
An interactive \code{plotly} object representing the survival plot.
}
\description{
This function generates an interactive survival plot using \code{plotly}, including optional confidence bands.
}
\examples{
# Example data
left <- c(2, 4, 6, 8)
right <- c(4, 6, 8, NA)

# Create intervals with bootstrap
intervals <- TNBintervals(left, right, nboot = 10)

# Interactive plot
survivalTB:::plot.TBL(intervals, conf = TRUE, conf.level = 0.95)
}
